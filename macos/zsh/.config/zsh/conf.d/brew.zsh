_zsh_test_cmds 'brew' && _zsh_add_shortcuts --abbr \
    'bup      % brew update' \
    'bu       % brew upgrade' \
    'bi       % brew install' \
    'bl       % brew list' \
    'ble      % brew leaves' \
    'bo       % brew outdated' \
    'bin      % brew info' \
    'br       % brew uninstall' \
    'ba       % brew autoremove' \
    'bun      % brew uninstall' \
    'bre      % brew reinstall' \
    'bs       % brew search' \
    'bd       % brew desc' \
    'bt       % brew tap' \
    'but      % brew untap' \
    'bcle     % brew cleanup' \
    'bco      % brew outdated --cask' \
    'boc      % brew outdated --cask' \
    'bcu      % brew upgrade --cask' \
    'buc      % brew upgrade --cask' \
    'bci      % brew install --cask' \
    'bic      % brew install --cask' \
    'bcl      % brew list --cask' \
    'blc      % brew list --cask' \
    'bcin     % brew info --cask' \
    'binc     % brew info --cask' \
    'bcr      % brew uninstall --cask' \
    'brc      % brew uninstall --cask' \
    'bcun     % brew uninstall --cask' \
    'bunc     % brew uninstall --cask' \
    'bcre     % brew reinstall --cask' \
    'brec     % brew reinstall --cask' \
    'bcd      % brew desc --cask' \
    'bdc      % brew desc --cask' \
    'bbd      % brew bundle dump' \
    'bbl      % brew bundle list' \
    'bbla     % brew bundle list --all' \
    'bbf      % brew bundle --file=' \
    'bblf     % brew bundle list --file=' \
    'bblaf    % brew bundle list --all --file='
