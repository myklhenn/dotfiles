_zsh_test_cmds 'git' && _zsh_add_shortcuts --abbr \
    'ga     % git add' \
    'gaa    % git add --all' \
    'gb     % git branch' \
    'gbd    % git branch --delete' \
    'gc     % git checkout' \
    'gcb    % git checkout -b' \
    'gcm    % git commit' \
    'gcmm   % git commit -m' \
    'gf     % git fetch' \
    'gp     % git push' \
    'gpo    % git push origin' \
    'gpom   % git push origin master' \
    'gpl    % git pull' \
    'gplo   % git pull origin' \
    'gplom  % git pull origin master' \
    'gs     % git status' \
    'gss    % git status -s'
